# Mach2 — dépendances Python
requests>=2.31
trafilatura>=1.8
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.11
# Optionnel — rendu JS (installé séparément, + `playwright install chromium`)
# playwright>=1.40
